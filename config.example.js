/**
 * Runtime Configuration Template
 *
 * Copy this file to config.js and update with your Convex deployment URL.
 *
 * To get your URL:
 *   1. Run `npx convex dev` to start/create your dev deployment
 *   2. Copy the URL from the output or from .env.local
 *
 * config.js is gitignored so your URL won't be committed.
 */
window.SCORCHED_EARTH_CONFIG = {
    // Replace with your Convex deployment URL
    // Dev URL from `npx convex dev` or prod URL from `npx convex deploy`
    CONVEX_URL: 'https://YOUR_DEPLOYMENT.convex.cloud'
};
