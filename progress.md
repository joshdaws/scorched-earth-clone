Original prompt: Upgrade game graphical assets with ChatGPT Image Gen 2 and improve tanks so the turret feels embedded rather than overlaid.

2026-04-22:
- Generated and processed cohesive weapon icons, UI placeholders, dirt particle, and split player/enemy tank body and turret sprites.
- Added manifest entries and renderer support for default player/enemy split tank sprites.
- Verified with `npm run check`, `npm run build`, and a Vite/Playwright gameplay screenshot.
- Polished the shared canvas Button renderer and gameplay FIRE button with layered bevels, highlights, stronger armed/pressed states, and menu hover handling.
- Filed `scorched-earth-7wu` to replace the old test explosion sprite sheet with a fresh generated animation before wiring sprite-frame explosions.
- Follow-up correction: made the FIRE button polish much more visible with a bright inset launch surface, heavier hardware frame, stronger glow/charge rail, and shifted it left to avoid crowding the pause button. Also strengthened shared Button interiors so menu buttons read as filled controls rather than plain stroked rectangles.
- Broader UI polish pass: upgraded menu stat/resource tiles, gameplay HUD chrome, health bars, wind rail, weapon dock, weapon slots, and angle readout so the non-title UI matches the quality of the new title/background. Kept this pass canvas-native; no new bitmap UI assets were necessary.
- Top HUD follow-up: upgraded the player info, enemy health, and turn/wind panels to sharper chamfered cockpit-style instrument frames, added a cash chip, and made enemy status read as a complete module.
- Explosion animation pass: generated a new 4x4 transparent synthwave explosion atlas with the Image API, replaced the old test sprite sheet, added manifest frame metadata, and rendered frame-based explosions over the existing procedural shockwave/particles with static PNG fallbacks.
- Explosion correction: user rejected the generated 4x4 animation because frames drift and get cut off. Reverted runtime explosions to the previous static small/medium/large overlay path and filed `scorched-earth-bob` for a future centered atlas.
- HUD alignment tweak: locked the enemy health module to the same top Y as the player info panel so the two top-corner modules align on a shared horizontal baseline.
- No open TODOs from the button polish pass.

2026-04-24:
- Architecture modernization roadmap created under bead `scorched-earth-qtq`, with child tasks for impact extraction, gameplay events, generated asset metadata, Tron-style terrain de-res effects, angle-of-repose dirt physics, main.js splitting, iOS hardening, and visual regression scenes.
- Began foundation work on `scorched-earth-qtq.2`: added a lightweight gameplay event bus and wired projectile impact, impact resolved, tank damaged, and terrain changed events from existing impact paths without changing current gameplay behavior.
- Began `scorched-earth-qtq.3`: added asset metadata normalization/validation for generated art, including anchors, pivots, frame grids, safe bounds, scale policies, and source provenance. Added sample turret pivot and explosion anchor metadata to the manifest.
- Browser smoke found startup could hang when external Google font loading is blocked. Filed and fixed `scorched-earth-qtq.7.1` by adding a timeout fallback around title font preload so the game can initialize offline/local.
- Completed `scorched-earth-qtq.1`: extracted projectile impact resolution into `js/impactResolution.js`, leaving `main.js` to inject live state and render/effect hooks. Added resolver unit tests for lifecycle events, damage events, terrain/no-terrain paths, chain reactions, and Liquid Dirt terrain mutation.
- Verified the resolver extraction with full check/build and a 30s browser smoke against the physics sandbox; canvas rendered successfully with only the known external-service console errors ignored.
- Completed `scorched-earth-qtq.4`: added `js/terrainDerezEffect.js`, captures pre/post terrain height samples in `destroyTerrainAt`, emits destruction samples on terrain events, and renders anchored Tron-style neon pixel chunks over the crater. Verified with unit tests, full check/build, the develop-web-game client, and a real-shot physics sandbox smoke that captured the effect immediately after `TERRAIN_CHANGED`.
- Began PixiJS terrain direction under `scorched-earth-qtq.4.2`: added `pixi.js` as a lazy-loaded dependency, introduced a heightmap-derived terrain cell snapshot helper, and created a GPU-backed Pixi terrain layer that composites chunky neon terrain cells into the existing Canvas 2D gameplay pass. Impact events now include removed terrain cells so de-res fragments originate from the actual destroyed grid cells, while Canvas terrain remains the fallback if Pixi cannot initialize.
- Continued under `scorched-earth-qtq.9`: added a persistent `TerrainCellGrid` bridge that owns cell occupancy and circular blast removal, exports compatible stepped heights back into the legacy heightmap, and feeds the Pixi terrain renderer directly. Restyled the terrain to a full magenta/cyan lattice with depth glow instead of only a neon cap line.
- Terrain pass follow-up: made the Pixi terrain body fully opaque, simplified the lattice to one uniform line pass, brightened only the top contour, and faded the grid with depth so the terrain reads closer to the provided mockup instead of transparent overdraw.
- Added `TerrainCellGrid.settleUnsupported()`: destructive impacts now remove cells inside the blast radius, compact unsupported cells downward, redistribute steep adjacent columns into a stable stepped slope, and only then export heights for legacy systems. Added unit coverage for floating-cell collapse, slope redistribution, and side cliff impacts.
- Browser smoke used the MCP Playwright fallback because the repo lacks a local `playwright` dependency for the `develop-web-game` client. Filed `scorched-earth-qtq.10` to add a repo-native visual smoke script/dependency.
- Remaining terrain modernization: `scorched-earth-qtq.9.1` still needs projectile collision queries, tank placement/resting, and other terrain consumers moved directly onto `TerrainCellGrid` so the heightmap export can become compatibility-only.
- De-rez performance/feel pass: replaced the prior flashy Pixi particle spray with removed-cell debris that starts at exact TerrainCellGrid cell centers, uses terrain/grid-derived tints, displaces outward briefly, and fades quickly. Lowered fragment caps from 520-per-impact/1400-active to 220-per-impact/520-active and added TestAPI hooks for deterministic terrain-destruction visual smokes.
- Projectile smoothness pass: capped fixed-timestep catch-up to 2 physics updates per rendered frame and drop stale backlog after hitches so shots do not pause and then sprint forward. Added unit coverage for the fixed-update planner and smoke-tested a fired shot in the physics sandbox.
- Follow-up filed as `scorched-earth-qtq.12`: add true projectile render interpolation plus frame hitch metrics if additional smoothness work is needed after the catch-up cap.
- Performance/iOS audit completed in `PERFORMANCE_IOS_AUDIT.md`. Key findings: initial app path is too large, release output copies non-production assets and sourcemaps, iOS needs render quality profiles/DPR caps, perf telemetry is missing, Pixi terrain rebuilds need dirty-region caching, assets should lazy-load by game state, and iOS release automation should be formalized. Filed follow-ups `scorched-earth-qtq.13`, `scorched-earth-qtq.7.3`, `scorched-earth-qtq.14`, `scorched-earth-qtq.9.1.1`, `scorched-earth-qtq.15`, and `scorched-earth-qtq.7.4`.
- Completed `scorched-earth-qtq.13`: replaced blind `cp -r assets www/` with a production asset copy script, disabled sourcemaps for default release builds, added `build:debug`, added release size/forbidden-path budget checks, and documented runtime/source-only asset policy. `npm run build` now reports `www` at 4.89 MB and excludes references, TextMesh Pro, `.DS_Store`, and sourcemaps.
- Completed `scorched-earth-qtq.7.3`: added persistent low/balanced/high render quality profiles with mobile/iOS defaulting to balanced, DPR caps for the gameplay canvas, quality-aware CRT/title/Pixi settings, an Options panel quality control, and `TestAPI` hooks. Verified with targeted render-quality tests, full `npm run check`, `npm run build`, and a browser smoke that switched profiles at runtime.
- Completed `scorched-earth-qtq.14`: added rolling runtime performance telemetry for frame intervals, fixed-step updates, dropped backlog, terrain impact timing, Pixi terrain rebuild/render timing, effect counts, fragment counts, and browser memory when available. Exposed reset/snapshot/budget checks through `TestAPI`, added `npm run perf:budget`, documented title/gameplay/projectile/impact smoke flows, and verified with tests, full check/build, and a live terrain-impact smoke.
- Completed `scorched-earth-qtq.9.1.1`: split Pixi terrain into cached terrain chunks, invalidates only impacted/settling chunk ranges after localized crater edits, keeps de-rez particles in a separate layer, and exposes chunk/cache gauges through performance metrics. Live smoke confirmed a 9-chunk terrain rebuilt 5 dirty chunks for a crater while fragments stayed aligned.
- Completed `scorched-earth-qtq.15`: added runtime asset groups with manifest-side group and approximate memory metadata, changed startup to load only boot/title assets, lazy-loads gameplay/collection/shop/editor/supply-drop groups on state entry, and exposes grouped cache status through `TestAPI`. Browser smoke confirmed menu startup cached only title art, while physics sandbox loaded gameplay without loading collection or supply-drop art.
- Completed `scorched-earth-qtq.7.4`: added `npm run ios:check` to run the release build/budget, validate Capacitor/iOS project files, verify icons/splash assets, and run Capacitor sync by default. Added an iOS release readiness doc with device matrix, safe-area/orientation/audio/haptics/network/performance checklist, and TestFlight validation steps.
- Began `scorched-earth-qtq.5`/`scorched-earth-qtq.9.1` terrain follow-up: added grid-backed terrain height/contact/collision helpers, routed projectile collision and tank placement/reseating through TerrainCellGrid, and added focused tests for side-cavity collision reads plus tank reseating from persistent grid contact height.
- Continued `scorched-earth-qtq.4.1`: tuned Pixi terrain de-rez so each removed terrain cell can emit a small anchored chunky fragment cluster, increasing small-impact density and data-shatter readability while keeping the existing active-fragment caps.
- Continued `scorched-earth-qtq.9.1`: added grid-backed terrain surface/slope helpers and routed rolling projectiles, bounce slope, AI terrain reasoning, trajectory previews, and the Canvas terrain fallback through TerrainCellGrid reads.
